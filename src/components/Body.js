import RestaurantCardContainer from "./RestaurantCardContainer";

const Body = () => {
  return (
    <div className="body">
      <RestaurantCardContainer />
    </div>
  );
};

export default Body;
